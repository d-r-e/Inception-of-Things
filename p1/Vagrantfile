Vagrant.configure("2") do |config|
    config.vm.box = "generic/alpine317"
    config.vm.define "darodrigS" do |server|
        server.vm.hostname = "darodrigS"
        server.vm.network "private_network", ip: "192.168.56.110"
        server.vm.provider "virtualbox" do |v|
            v.name = "darodrigS"
            v.memory = 512
            v.cpus = 1
        end
        server.vm.provision "shell", inline: <<-SHELL
        curl -sfL https://get.k3s.io | K3S_KUBECONFIG_MODE="644" K3S_TOKEN="420000000000000000000042" INSTALL_K3S_EXEC="--flannel-iface eth1" sh -s -
        echo 'alias k="sudo k3s kubectl"' >> ~/.bashrc
        SHELL
    end

    config.vm.define "darodrigSW" do |worker|
        worker.vm.hostname = "darodrigSW"
        worker.vm.network "private_network", ip: "192.168.56.111"
        worker.vm.provider "virtualbox" do |v|
            v.name = "darodrigSW"
            v.memory = 512
            v.cpus = 1
        end
        worker.vm.provision "shell", inline: <<-SHELL
        curl -sfL https://get.k3s.io | K3S_KUBECONFIG_MODE="644" K3S_URL=https://192.168.56.110:6443 K3S_TOKEN="420000000000000000000042" INSTALL_K3S_EXEC="--flannel-iface eth1" sh -s -
        SHELL
    end
    
end